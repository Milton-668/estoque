server:
  error:
    include-stacktrace: on_param
##?trace=true habilita o stacktrace
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/estoque?createDatabaseIfNotExist=true

    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

  main:
    allow-bean-definition-overriding: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG